<div class="photo_albums">
  <% object.items.each_slice(3) do |slice| %>
    <ul>
      <% slice.each do | album | %>
        <li>
          <% if album.images.any? %>
            <div class="image">
              <%= link_to album_thumbnail(album.images.first.thumbnail), album.link %>
            </div>
          <% end %>
          <div class='description'>
            <p class='title'>
              <%= link_to strip_tags(album.title), album.link %>
              <span class="count">(12)</span>
            </p>
            <p class='date'>
              <%= l DateTime.parse(album.since), :format => "%d %B %Y" %>
              <span class='time'>
                <%= l DateTime.parse(album.since), :format => "%H:%M" %>
              </span>
            </p>
          </div>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>

<% if object.pagination %>
  <% @array = object.items %>
  <% @array = Kaminari.paginate_array(@array, :total_count => object.pagination.total_count).page(object.pagination.current_page).per(object.pagination.per_page) %>
  <%= paginate @array, :param_name => object.pagination.param_name, :outer_window => 3 %>
<% end %>
