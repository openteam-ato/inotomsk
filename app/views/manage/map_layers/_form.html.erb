<%= simple_form_for [:manage, @map_layer], html: {class: '.form-vertical' } do |f| %>
  <%= f.input :title %>
  <%= f.input :icon, :as => :file %>
  <%= f.input :hover_icon, :as => :file %>
  <%= f.submit %>
<% end %>
